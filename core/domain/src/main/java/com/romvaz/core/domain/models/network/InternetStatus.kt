package com.romvaz.core.domain.models.network

// Enum to read network connectivity status for code management
enum class InternetStatus {
    LOST_CONNECTION, HAVE_CONNECTION, UNAVAILABLE_CONNECTION
}
